import { test, expect } from '@playwright/test'; test.describe('Page Visual Regression', () => { test('Homepage Layout', async ({ page }) => { await page.goto('/'); await page.waitForLoadState('networkidle'); await expect(page).toHaveScreenshot('homepage-full.png'); }); test('Dashboard Layout', async ({ page }) => { await page.goto('/dashboard'); await page.waitForLoadState('networkidle'); await expect(page).toHaveScreenshot('dashboard-full.png'); }); });
